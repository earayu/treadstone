name: Update Prod Image

on:
  workflow_run:
    workflows: ["Release"]
    types: [completed]

permissions:
  contents: write

jobs:
  update-prod-image:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get release tag
        id: tag
        run: |
          git fetch --tags origin
          # workflow_run.head_sha is the pre-bump commit for Release (workflow_dispatch).
          # bump-and-tag pushes a new commit + tag, so exact-match on head_sha fails.
          # After checkout main, HEAD is the release tip; nearest tag is the release tag.
          TAG=$(git describe --tags --abbrev=0 HEAD 2>/dev/null || echo "")
          if [ -z "$TAG" ] || [[ "$TAG" != v* ]]; then
            echo "No semver tag found on main — skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "version=$TAG" >> "$GITHUB_OUTPUT"
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Update values-prod.yaml
        if: steps.tag.outputs.skip == 'false'
        run: python scripts/update_prod_image.py "${{ steps.tag.outputs.version }}"

      - name: Commit and push
        if: steps.tag.outputs.skip == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/treadstone/values-prod.yaml deploy/treadstone-web/values-prod.yaml
          git diff --cached --quiet \
            || git commit -m "chore: update prod image to ${{ steps.tag.outputs.version }}"
          git push
