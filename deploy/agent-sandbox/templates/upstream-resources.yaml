{{/*
Load the upstream CRDs and cluster-scoped resources from the vendored files.
The controller Deployment is rendered separately so Helm values can control
its image, replica count, and resources while keeping the upstream manifests
close to their published form.
*/}}

---
{{ .Files.Get "upstream/manifest.yaml" }}
---
{{ .Files.Get "upstream/extensions.yaml" }}
