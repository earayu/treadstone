apiVersion: v1
kind: Service
metadata:
  name: {{ include "treadstone-web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "treadstone-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "treadstone-web.selectorLabels" . | nindent 4 }}
