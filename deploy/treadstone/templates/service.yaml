apiVersion: v1
kind: Service
metadata:
  name: {{ include "treadstone.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "treadstone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "treadstone.selectorLabels" . | nindent 4 }}
